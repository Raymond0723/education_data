# mysql 無法連線(mysql command line跳窗)的解決方法 

方法1.打開 cmd => 
輸入
```
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql" -u root -p  
```
或     

方法2.系統環境變數 裡面輸入輸入以下路徑(如果安裝預設路徑是以下)
```
"C:\Program Files\MySQL\MySQL Server 8.0\bin" 
```